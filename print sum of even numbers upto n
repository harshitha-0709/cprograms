#include <stdio.h>

int main() {
    int n,count,sum;
    scanf("%d",&n);
    count=1;
    sum=0;
    while(count<=n)
    {
        if(count%2==0)
        {
            printf("+%d",count);
        sum=sum+count;
        }
           count++;
    }
        printf("=%d",sum);
    return 0;
}
