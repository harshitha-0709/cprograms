#include <stdio.h>

int main() {
    int num,i,flag,m;
    scanf("%d",&m);
    for(num=1;num<=m;num++){
    for(i=2,flag=0;i*i<=num;i++)
    {
        if(num%i==0)
        flag=1;
    }
    if(flag==0&&(num!=1)){
    printf("%d ",num);
    }
    }
    return 0;
}
