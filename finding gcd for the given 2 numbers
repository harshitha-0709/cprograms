#include <stdio.h>

int main() {
  int a,b,i,gcd,min;
  scanf("%d%d",&a,&b);
  gcd=1;
  if(a<b){
      min=a;
  }
     else{
         min=b;
     }
          for(i=1,gcd=1;i<=min;i++){
          if(a%i==0 && b%i==0){
              gcd=i;
         }
          }
     printf("%d",gcd);
}
